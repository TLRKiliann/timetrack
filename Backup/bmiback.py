#!/usr/bin/python3
# -*- coding: utf-8 -*-


import os
import shutil


def bmiCall():
    """
        Copy all bmiX.txt files.
    """
    try:
        if os.path.exists('./calBmi/bmi.txt'):
            shutil.copy('./calBmi/bmi.txt',
                './Backup/old/oldfiles1/bmi.txt')
            print("[Copy] bmi.txt copied.")
    except FileNotFoundError as fnf_bmi1:
        print("[!] Not found", fnf_bmi1)

    try:
        if os.path.exists('./calBmi/bmi2.txt'):
            shutil.copy('./calBmi/bmi2.txt',
                './Backup/old/oldfiles2/bmi2.txt')
            print("[Copy] bmi2.txt copied.")
    except FileNotFoundError as fnf_bmi2:
        print("[!] Not found", fnf_bmi2)

    try:
        if os.path.exists('./calBmi/bmi3.txt'):
            shutil.copy('./calBmi/bmi3.txt',
                './Backup/old/oldfiles3/bmi3.txt')
            print("[Copy] bmi3.txt copied.")
    except FileNotFoundError as fnf_bmi3:
        print("[!] Not found", fnf_bmi3)

    try:
        if os.path.exists('./calBmi/bmi4.txt'):
            shutil.copy('./calBmi/bmi4.txt',
                './Backup/old/oldfiles4/bmi4.txt')
            print("[Copy] bmi4.txt copied.")
    except FileNotFoundError as fnf_bmi4:
        print("[!] Not found", fnf_bmi4)

    try:
        if os.path.exists('./calBmi/bmi5.txt'):
            shutil.copy('./calBmi/bmi5.txt',
                './Backup/old/oldfiles5/bmi5.txt')
            print("[Copy] bmi5.txt copied.")
    except FileNotFoundError as fnf_bmi5:
        print("[!] Not found", fnf_bmi5)

    try:
        if os.path.exists('./calBmi/bmi6.txt'):
            shutil.copy('./calBmi/bmi6.txt',
                './Backup/old/oldfiles6/bmi6.txt')
            print("[Copy] bmi6.txt copied.")
    except FileNotFoundError as fnf_bmi6:
        print("[!] Not found", fnf_bmi6)

    try:
        if os.path.exists('./calBmi/bmi7.txt'):
            shutil.copy('./calBmi/bmi7.txt',
                './Backup/old/oldfiles7/bmi7.txt')
            print("[Copy] bmi7.txt copied.")
    except FileNotFoundError as fnf_bmi7:
        print("[!] Not found", fnf_bmi7)

    try:
        if os.path.exists('./calBmi/bmi8.txt'):
            shutil.copy('./calBmi/bmi8.txt',
                './Backup/old/oldfiles8/bmi8.txt')
            print("[Copy] bmi8.txt copied.")
    except FileNotFoundError as fnf_bmi8:
        print("[!] Not found", fnf_bmi8)

    try:
        if os.path.exists('./calBmi/bmi9.txt'):
            shutil.copy('./calBmi/bmi9.txt',
                './Backup/old/oldfiles9/bmi9.txt')
            print("[Copy] bmi9.txt copied.")
    except FileNotFoundError as fnf_bmi9:
        print("[!] Not found", fnf_bmi9)

    try:
        if os.path.exists('./calBmi/bmi10.txt'):
            shutil.copy('./calBmi/bmi10.txt',
                './Backup/old/oldfiles10/bmi10.txt')
            print("[Copy] bmi10.txt copied.")
    except FileNotFoundError as fnf_bmi10:
        print("[!] Not found", fnf_bmi10)

    try:
        if os.path.exists('./calBmi/bmi11.txt'):
            shutil.copy('./calBmi/bmi11.txt',
                './Backup/old/oldfiles11/bmi11.txt')
            print("[Copy] bmi11.txt copied.")
    except FileNotFoundError as fnf_bmi11:
        print("[!] Not found", fnf_bmi11)

    try:
        if os.path.exists('./calBmi/bmi12.txt'):
            shutil.copy('./calBmi/bmi12.txt',
                './Backup/old/oldfiles12/bmi12.txt')
            print("[Copy] bmi12.txt copied.")
    except FileNotFoundError as fnf_bmi12:
        print("[!] Not found", fnf_bmi12)

    try:
        if os.path.exists('./calBmi/bmi13.txt'):
            shutil.copy('./calBmi/bmi13.txt',
                './Backup/old/oldfiles13/bmi13.txt')
            print("[Copy] bmi13.txt copied.")
    except FileNotFoundError as fnf_bmi13:
        print("[!] Not found", fnf_bmi13)

    try:
        if os.path.exists('./calBmi/bmi14.txt'):
            shutil.copy('./calBmi/bmi14.txt',
                './Backup/old/oldfiles14/bmi14.txt')
            print("[Copy] bmi14.txt copied.")
    except FileNotFoundError as fnf_bmi14:
        print("[!] Not found", fnf_bmi14)

    try:
        if os.path.exists('./calBmi/bmi15.txt'):
            shutil.copy('./calBmi/bmi15.txt',
                './Backup/old/oldfiles15/bmi15.txt')
            print("[Copy] bmi15.txt copied.")
    except FileNotFoundError as fnf_bmi15:
        print("[!] Not found", fnf_bmi15)

    try:
        if os.path.exists('./calBmi/bmi16.txt'):
            shutil.copy('./calBmi/bmi16.txt',
                './Backup/old/oldfiles16/bmi16.txt')
            print("[Copy] bmi16.txt copied.")
    except FileNotFoundError as fnf_bmi16:
        print("[!] Not found", fnf_bmi16)

    try:
        if os.path.exists('./calBmi/bmi17.txt'):
            shutil.copy('./calBmi/bmi17.txt',
                './Backup/old/oldfiles17/bmi17.txt')
            print("[Copy] bmi17.txt copied.")
    except FileNotFoundError as fnf_bmi17:
        print("[!] Not found", fnf_bmi17)

    try:
        if os.path.exists('./calBmi/bmi18.txt'):
            shutil.copy('./calBmi/bmi18.txt',
                './Backup/old/oldfiles18/bmi18.txt')
            print("[Copy] bmi18.txt copied.")
    except FileNotFoundError as fnf_bmi18:
        print("[!] Not found", fnf_bmi18)

    try:
        if os.path.exists('./calBmi/bmi19.txt'):
            shutil.copy('./calBmi/bmi19.txt',
                './Backup/old/oldfiles19/bmi19.txt')
            print("[Copy] bmi19.txt copied.")
    except FileNotFoundError as fnf_bmi19:
        print("[!] Not found", fnf_bmi19)

    try:
        if os.path.exists('./calBmi/bmi20.txt'):
            shutil.copy('./calBmi/bmi20.txt',
                './Backup/old/oldfiles20/bmi20.txt')
            print("[Copy] bmi20.txt copied.")
    except FileNotFoundError as fnf_bmi20:
        print("[!] Not found", fnf_bmi20)

    try:
        if os.path.exists('./calBmi/bmi21.txt'):
            shutil.copy('./calBmi/bmi21.txt',
                './Backup/old/oldfiles21/bmi21.txt')
            print("[Copy] bmi21.txt copied.")
    except FileNotFoundError as fnf_bmi21:
        print("[!] Not found", fnf_bmi21)

    try:
        if os.path.exists('./calBmi/bmi22.txt'):
            shutil.copy('./calBmi/bmi22.txt',
                './Backup/old/oldfiles22/bmi22.txt')
            print("[Copy] bmi22.txt copied.")
    except FileNotFoundError as fnf_bmi22:
        print("[!] Not found", fnf_bmi22)

    try:
        if os.path.exists('./calBmi/bmi23.txt'):
            shutil.copy('./calBmi/bmi23.txt',
                './Backup/old/oldfiles23/bmi23.txt')
            print("[Copy] bmi23.txt copied.")
    except FileNotFoundError as fnf_bmi23:
        print("[!] Not found", fnf_bmi23)

    try:
        if os.path.exists('./calBmi/bmi24.txt'):
            shutil.copy('./calBmi/bmi24.txt',
                './Backup/old/oldfiles24/bmi24.txt')
            print("[Copy] bmi24.txt copied.")
    except FileNotFoundError as fnf_bmi24:
        print("[!] Not found", fnf_bmi24)
